@inject HttpClient Http
@page "/"

<PageTitle>Index</PageTitle>

<h1>Hello, world!</h1>

Welcome to your new app.

@if(message == null){
    <p>Waiting for message...</p>
}else{
    <p>@message</p>
}

@code
{
    string message = null;
    protected override async Task OnInitializedAsync()
    {
        message = await Http.GetStringAsync(
            "https://azurefunctiontest-20221113.azurewebsites.net/api/Function1"
        );
    }
}